type: cloudauth

status:
  class: extension
  stability:
    alpha: [extension]
  distributions:
  - contrib
  codeowners:
    active: [hsookim]

tests:
  goleak:
    ignore:
      top:
        - "net/http.(*persistConn).writeLoop"
        - "internal/poll.runtime_pollWait"
  config:
