# Use this changelog template to create an entry for release notes.
# If your change doesn't affect end users, such as a test fix or a tooling change,
# you should instead start your pull request title with [chore] or use the "Skip Changelog" label.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. filelogreceiver)
component: prometheusreceiver

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: add SharedGatherer to report the discovery and scrape registries of every running receiver

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [621]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  Each receiver's registry is private, so signals such as prometheus_target_scrape_pool_targets were
  only reachable by enabling the per-receiver api_server endpoint. SharedGatherer merges the
  registries of all running receivers behind one prometheus.Gatherer, letting a single endpoint or
  exporter report them. The set is resolved at gather time so receivers starting or stopping later
  need no re-wiring, and the existing receiver label keeps series from different instances distinct.
